apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# The external identity provider as a cluster object: a namespace and an ExternalName
# Service pointing at the host (update-setup-03). Nothing runs here.
resources:
- namespace.yaml
- service.yaml
