apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# The namespace all monitoring parts share. Its own part, because kustomize does not load
# files from outside a kustomization's directory.
resources:
- namespace.yaml
