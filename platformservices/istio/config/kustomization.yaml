apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# IngressClass "istio", the wildcard gateway certificate (needs cert-manager/config), and
# mesh-wide tracing to the OpenTelemetry Collector (needs the monitoring part, update-setup-05)
resources:
- ingressclass.yaml
- gateway-certificate.yaml
- telemetry.yaml
